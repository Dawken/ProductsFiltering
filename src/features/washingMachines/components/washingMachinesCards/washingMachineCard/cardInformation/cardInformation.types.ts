import { FeatureEnum } from '../../../../../../types/filtering/featureEnum'

type Props = {
    model: string
    type: string
    capacity: string
    color: string
    dimensions: string
    features: FeatureEnum[]
}

export type { Props }
